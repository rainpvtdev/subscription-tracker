# Database configuration
# These values are populated automatically by the Replit PostgreSQL integration
# DATABASE_URL=postgres://username:password@hostname:port/database_name
# PGHOST=hostname
# PGUSER=username
# PGPASSWORD=password
# PGDATABASE=database_name
# PGPORT=port
# Session secret
#SESSION_SECRET=your_session_secret

# Data migration (optional)
#STORAGE_PATH=./data
#BACKUP_PATH=./backup
#BACKUP_FILES=false


EMAIL_HOST =yourhost
EMAIL_USER=your-gmail@gmail.com
EMAIL_PASS=your-app-specific-password
FRONTEND_URL=http://localhost:8080  # or your actual frontend URL

# Data storage configuration
STORAGE_PATH=./data
BACKUP_PATH=./backup
# Set to 'true' to keep a backup of processed files instead of deleting them
BACKUP_FILES=false